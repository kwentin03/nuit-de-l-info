<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600&display=swap" rel="stylesheet">
    <title>
        <%= title %>
    </title>
</head>

<body>

    <header>
        <h1 style="font-family: 'Nunito', sans-serif; font-weight: 600;">
            S'ENGAGER ET LIBÉRER
        </h1>
        <nav style="font-family: 'Nunito', sans-serif; font-weight: 600; display: flex; gap: 20px;">
            <a href="#context">Contexte</a>
            <a href="#interet">Intérêt</a>
            <a href="#learn">Learn</a>
            <a href="#terminal">Terminal</a>
        </nav>
    </header>

    <div class="container">

        <div id="context" class="section">
            <h2>
                <%= sections.context.title %>
            </h2>
            <p>
                <%= sections.context.content %>
            </p>
        </div>

        <div id="interet" class="section">
            <h2>
                <%= sections.interet.title %>
            </h2>
            <p>
                <%= sections.interet.content %>
            </p>
        </div>

        <div id="learn" class="section">
            <h2>Apprendre en s'amusant !</h2>
            <div class="learn-grid">
                <% learnCards.forEach(card=> { %>
                    <div class="learn-card">
                        <%= card %>
                    </div>
                    <% }); %>
            </div>
        </div>

        <div id="terminal" class="section">
            <h2>Mini système Linux</h2>
            <div class="terminal">
                <div class="terminal-output" id="output">Bienvenue dans le mini système Linux simulé.
                    Tapez "help" pour commencer.</div>

                <div class="terminal-input">
                    <span>$</span>
                    <input type="text" id="cmd" placeholder="Entrez une commande..." autocomplete="off">
                </div>
            </div>
        </div>

    </div>

    <script src="/js/terminal.js"></script>

</body>

</html>